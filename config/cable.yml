development:
  adapter: async

test:
  adapter: test

production:
  # No Redis on Render free. CounterDeck has no real-time broadcast features,
  # so the in-process async adapter is sufficient and costs nothing.
  adapter: async
