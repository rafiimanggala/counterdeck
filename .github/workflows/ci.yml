name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: counterdeck
          POSTGRES_PASSWORD: counterdeck
          POSTGRES_DB: counterdeck_test
        ports:
          - 5435:5432
        options: >-
          --health-cmd "pg_isready -U counterdeck"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "4.0.2"
          bundler-cache: true

      - name: Run CI suite (style, security, tests)
        run: bin/ci
